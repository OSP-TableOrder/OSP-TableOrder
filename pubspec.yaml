name: table_order
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  carousel_slider: ^5.0.0
  smooth_page_indicator: ^1.1.0
  cupertino_icons: ^1.0.8
  webview_flutter: ^4.13.0
  provider: ^6.1.5+1
  app_links: ^3.5.1 # Flutter DeepLink 처리

  qr_flutter: ^4.1.0
  screenshot: ^3.0.0
  permission_handler: ^11.0.1
  path_provider: ^2.1.1
  gal: ^2.3.2

  # Firebase
  firebase_core: ^3.10.0
  firebase_auth: ^5.3.5
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.0.0

  # Image handling
  image_picker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
